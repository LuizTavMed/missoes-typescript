export class Pessoas {
  nome: string
  idade: number
  email: string
  id: string
  constructor (nome: string, idade: number, email: string, id: string) {
    this.nome = nome
    this.idade = idade
    this.email = email
    this.id = id
  }
}
